<!-- res/drawable/rounded_edittext_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- Màu nền của EditText (trắng) -->
    <stroke
        android:width="2dp"
        android:color="#F29D38" /> <!-- Kích thước và màu viền -->
    <corners android:radius="20dp" /> <!-- Độ cong của góc -->
</shape>
